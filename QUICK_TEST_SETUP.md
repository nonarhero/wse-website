# 🚀 วิธี Setup สำหรับลูกค้าเทส - แบบง่ายที่สุด

## ✅ วิธีที่ 1: ใช้ Vercel Postgres (แนะนำ - ง่ายที่สุด)

**ข้อดี:**
- ไม่ต้อง setup MySQL server เอง
- ไม่ต้องจัดการ firewall
- Vercel จัดการให้หมด
- ใช้งานได้ทันที

### ขั้นตอน (5 นาที)

#### 1. สร้าง Database บน Vercel

1. ไปที่ **Vercel Dashboard** > Project `wse-website`
2. คลิกแท็บ **Storage**
3. คลิก **Create Database**
4. เลือก **Postgres** (ไม่ต้อง MySQL ก็ได้ - เราจะแก้ schema เล็กน้อย)
5. ตั้งชื่อ: `wse-db` (หรือชื่ออะไรก็ได้)
6. คลิก **Create**
7. **Copy Connection String** (จะได้ URL ประมาณ: `postgres://...`)

#### 2. แปลง Schema เป็น Postgres

**ไม่ต้องเปลี่ยน!** Prisma รองรับ Postgres แล้ว แค่เปลี่ยน `provider` ใน `schema.prisma`

แก้ไข `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"  // เปลี่ยนจาก "mysql"
  url      = env("DATABASE_URL")
}
```

#### 3. ตั้งค่า DATABASE_URL บน Vercel

1. ไปที่ **Settings** > **Environment Variables**
2. เพิ่ม Variable:
   - **Key:** `DATABASE_URL`
   - **Value:** วาง **Connection String** ที่ copy มา
   - **Environment:** เลือก **Production**, **Preview**, **Development**
3. คลิก **Save**

#### 4. Setup Database Schema

```powershell
# Pull environment variables
vercel env pull .env.local

# Generate Prisma Client
npx prisma generate

# Push schema to database
npx prisma db push

# สร้าง Admin User
npm run create-admin admin@wse.com "Password123" "Super Admin"
```

#### 5. Redeploy

```powershell
git add .
git commit -m "Switch to Postgres for testing"
git push
```

หรือคลิก **Redeploy** ใน Vercel Dashboard

---

## 🔄 วิธีที่ 2: ใช้ VPS + MySQL (ถ้าต้องการควบคุมเอง)

ใช้ตามคู่มือ `MYSQL_SERVER_SETUP.md` แต่ใช้เวลา setup มากกว่า

---

## 📋 Checklist วิธีที่ 1 (Vercel Postgres)

- [ ] สร้าง Postgres database บน Vercel Storage
- [ ] Copy connection string
- [ ] แก้ไข `prisma/schema.prisma` เป็น `postgresql`
- [ ] ตั้งค่า `DATABASE_URL` บน Vercel Environment Variables
- [ ] Pull env: `vercel env pull .env.local`
- [ ] Generate Prisma: `npx prisma generate`
- [ ] Push schema: `npx prisma db push`
- [ ] Create admin: `npm run create-admin`
- [ ] Redeploy
- [ ] ทดสอบ: https://your-app.vercel.app/admin/login

---

## 🎯 เปรียบเทียบ

| คุณสมบัติ | Vercel Postgres | VPS + MySQL |
|----------|----------------|-------------|
| **เวลา Setup** | 5 นาที | 30-60 นาที |
| **ความยาก** | ⭐ ง่ายมาก | ⭐⭐⭐ ปานกลาง |
| **ค่าใช้จ่าย** | ฟรี (ถึง limit) | ตาม VPS |
| **การจัดการ** | Vercel จัดการ | ต้องจัดการเอง |
| **Firewall** | ไม่ต้อง | ต้องตั้งค่า |
| **เหมาะกับ** | ทดสอบเร็ว | Production จริง |

---

## 💡 คำแนะนำ

**สำหรับลูกค้าเทส:** ใช้ **Vercel Postgres** (วิธีที่ 1)
- ง่าย เร็ว ไม่ต้อง setup เอง
- พอทดสอบเสร็จแล้ว ถ้าต้องการ MySQL จริง ค่อยย้ายทีหลัง

**สำหรับ Production:** ใช้ **VPS + MySQL** (วิธีที่ 2)
- ควบคุมได้เต็มที่
- ปรับแต่ง performance ได้
- Security ดีกว่า

---

## ❓ FAQ

**Q: Postgres กับ MySQL ต่างกันไหม?**
A: สำหรับโปรเจกต์นี้ ต่างกันน้อยมาก Prisma จัดการให้ แล้วสามารถย้ายกลับ MySQL ได้ในอนาคต

**Q: ต้องเปลี่ยน code ไหม?**
A: ไม่ต้อง แค่เปลี่ยน `provider` ใน `schema.prisma` จาก `mysql` เป็น `postgresql`

**Q: ข้อมูลจะหายไหม?**
A: Vercel Postgres เก็บข้อมูลจริง ไม่หาย (เว้นแต่ลบ database)

**Q: เปลี่ยนกลับ MySQL ได้ไหม?**
A: ได้ แต่ต้อง export/import ข้อมูลก่อน
