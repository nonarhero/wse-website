# 🔍 ตรวจสอบฟอร์มบทความ

## ❌ ปัญหา

ฟอร์มเนื้อหาไม่แสดง (ไม่เหมือนตอน dev)

---

## ✅ ฟอร์มใช้ Textarea ธรรมดา

**ฟอร์มเนื้อหา** (บรรทัด 169-179):
- ใช้ `<textarea>` ธรรมดา
- ไม่ใช่ rich text editor
- Rows: 20
- Class: `font-mono`

---

## 🔍 วิธีตรวจสอบ

### 1. ตรวจสอบ Browser Console

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Console**
3. หา error messages (มักจะเป็น red)

**Error ที่พบบ่อย:**
- `Failed to fetch categories` → API `/api/categories` พัง
- `Failed to fetch tags` → API `/api/tags` พัง
- JavaScript errors → Component ไม่ render

### 2. ตรวจสอบ Network Tab

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Network**
3. Refresh หน้า `/admin/articles/new`
4. หา requests ที่เป็น **500 Error** หรือ **Failed**

**API endpoints ที่ควรทำงาน:**
- `/api/categories` → ควรเป็น 200 OK
- `/api/tags` → ควรเป็น 200 OK

### 3. ตรวจสอบ Elements

1. เปิด Developer Tools (F12)
2. ไปที่แท็บ **Elements**
3. หา `<textarea>` ที่มี `placeholder="เขียนเนื้อหาบทความที่นี่"`
4. ตรวจสอบว่า element ถูก render หรือไม่

---

## 💡 ถ้ายังไม่เห็นฟอร์ม

**ลอง:**
1. Hard Refresh (Ctrl+Shift+R)
2. ตรวจสอบ Browser Console (F12 > Console)
3. ตรวจสอบ Network Tab (F12 > Network)
4. ตรวจสอบ Elements (F12 > Elements)

---

**บอกผลลัพธ์จากการตรวจสอบ!** 🔍
